{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */scripts/*.py) ruff check --fix \"$f\" && ruff format \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}

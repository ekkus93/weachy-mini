# Local model manifests

Commit small, reviewable metadata manifests here. Do not commit GGUF, ONNX, safetensors, tokenizer archives, generated indexes, credentials, or partially downloaded model files.

Every installable model manifest will eventually require a source revision, license, exact file size, SHA-256 hash, context limits, memory estimate, and device-compatibility information.
